@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using StudentManagement.Models;
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>User Profile</title>
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
          rel="stylesheet" />
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
          rel="stylesheet" />
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/4.3.0/mdb.min.css"
          rel="stylesheet" />
    <style>
        .card-registration .select-input.form-control[readonly]:not([disabled]) {
            font-size: 1rem;
            line-height: 2.15;
            padding-left: .75em;
            padding-right: .75em;
        }

        .card-registration .select-arrow {
            top: 13px;
        }
    </style>
</head>
<body>
    @if (ViewBag.User == null)
    {


        <input class="btn btn-primary" type="button" value="Login" onclick="window.location='/student/login'" />
    }
    else
    {
         Student s = ViewBag.User;
        <input class="btn btn-primary" type="button" value="Logout" onclick="window.location='/student/logout'" />

        <input class="btn btn-primary" type="button" value="Change Password" onclick="window.location='/student/changepassword'" />
        @if (s.RoleId == 1)
        {
            <input class="btn btn-primary" type="button" value="Edit Profile" onclick="window.location='/student/userprofile'" />
            @if (s.MajorId != null)
            {
                <input class="btn btn-primary" type="button" value="Change Class" onclick="window.location='/student/changeclass'" />
                <input class="btn btn-primary" type="button" value="Change Major" onclick="window.location='/student/changemajor'" />
            }
            else
            {
                <input class="btn btn-primary" name="registermajor" type="button" value="Register Major" onclick="window.location='/student/registermajor'" />
            }

        }


        @if (s.RoleId == 2)
        {
            <input class="btn btn-primary" type="button" value="StudentList" onclick="window.location='/admin/studentlist'" />
            <input class="btn btn-primary" type="button" value="MajorList" onclick="window.location='/admin/majorlist'" />
            <input class="btn btn-primary" type="button" value="ClassList" onclick="window.location='/admin/classlist'" />
        }
    }

</body>

<!-- MDB -->

<script type="text/javascript"
        src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/4.3.0/mdb.min.js"></script>
</html>